export default function bufferFromDataURI(dataURI: string) {
    // const [, mimeType, encoding, data] = dataURI.match(
    //     /^data:((?:[^,](?!,|;base64))*[^,])?(?:;(base64))?,(.+)/im
    // )
    // return {
    //     data: encoding
    //         ? Buffer.from(data, encoding)
    //         : data,
    //     mimeType
    // }
}
